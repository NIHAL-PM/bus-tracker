{
  "name": "ksrtc-bus-tracker",
  "version": "2.0.0",
  "description": "Live KSRTC bus tracking system with MongoDB",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "mongodb": "^6.3.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
